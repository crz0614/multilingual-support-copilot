import type {Metadata} from "next";import "./globals.css";import "./truth.css";export const metadata:Metadata={title:"Relay — Public Issue Triage Copilot",description:"Transparent public-issue triage with deterministic routing and explicit provider boundaries."};export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
